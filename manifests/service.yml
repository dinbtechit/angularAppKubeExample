apiVersion: v1
kind: Service
metadata:
    name: "myappdeve-31b5"
    labels:
        app: "myappdeve-31b5"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "myappdeve-31b5"