apiVersion: v1
kind: Service
metadata:
    name: "instapieaks-80d2"
    labels:
        app: "instapieaks-80d2"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "instapieaks-80d2"